
## 0.1.0+2
* Remove `window_manager` dependency and related code from the example.
* Add `autoMaximize` parameter to the sub-window example to demonstrate how to enter fullscreen mode on macOS/Windows after the first frame is rendered (which is necessary for the menu bar to hide on macOS).

## 0.1.0+1
* Update package names to `app.mylekha.extend_screen` and `app.mylekha.extend_screen_example` for Android.


## 0.1.0

* Initial release.
* Multi-window support for Windows, macOS, and Linux via `desktop_multi_window`.
* Dual-display / secondary-screen support for Android (POS devices).
